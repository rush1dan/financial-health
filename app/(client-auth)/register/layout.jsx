import { ReactNode } from "react"

export const metadata = {
    title: 'Register',
    description: 'Financial Health - Register',
}

export default function RegisterPageLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}