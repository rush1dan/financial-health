import { ReactNode } from "react"

export const metadata = {
    title: 'Login',
    description: 'Financial Health - Login',
}

export default function LoginPageLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}